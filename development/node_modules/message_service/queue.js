// a simple FIFO queue that just adds items
var events = require('events');
var util = require('util');

var Queue = function() {
    this.queue = [];
    events.EventEmitter.call(this);

    this.queue = function(obj) {
        this.queue.push(obj);
        this.emit('queue', msg);       
    }
    
    this.dequeue = function() {
        this.queue.shift();
        this.emit('dequeue', msg);       
    }
}
util.inherits(Queue, events.EventEmitter);



module.exports = {
    Queue: Queue
};
